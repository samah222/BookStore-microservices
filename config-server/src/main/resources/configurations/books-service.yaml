spring:
  application:
    name: books-service
    version: v1
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/books_service
    password: E15@root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'false'

server:
  port: '8081'

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
